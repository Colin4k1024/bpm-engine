//! API server stub. Full server in bpm-server-rest crate.
//! Run: cargo run -p bpm-server-rest

fn main() {
    println!("API server moved to bpm-server-rest. Run: cargo run -p bpm-server-rest");
}
